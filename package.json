{
  "name": "noeidelon",
  "type": "module",
  "main": "./build.ts",
  "scripts": {
    "build": "bun $BUNFLAGS .",
    "build:watch": "BUNFLAGS=--watch bun run build",
    "serve": "serve ./html",
    "dev": "run-p serve build:watch"
  },
  "devDependencies": {
    "bun-types": "latest",
    "npm-run-all2": "^6.0.6",
    "prettier": "^3.0.3"
  },
  "peerDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "glob": "^10.3.10",
    "serve": "^14.2.1"
  }
}
